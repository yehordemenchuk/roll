spring.application.name=GameStudioServer-Boy
# Čiarkou oddelené CORS origin-y (Docker / produkcia). Prázdne = predvolené lokálne Vite URL.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:}
spring.datasource.url=jdbc:postgresql://localhost:5432/gamestudiodb
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:12102021egorDem}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=${JWT_SECRET:super-secret-key-super-secret-key}
jwt.access_token.expiration_time=600000
jwt.refresh_token.expiration_time=10080000